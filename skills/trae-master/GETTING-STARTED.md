# Trae Master 框架使用指南

## 一、框架是什么

Trae Master 是一个 AI 驱动的研发工作流框架，模拟真实团队协作模式：Boss（当前AI）→ 组长 → 专员，按 PM → RD → QA 三阶段严格顺序执行需求。

核心理念：**AI 不是一个人干所有事，而是像一个真实团队一样分工协作。**

---

## 二、架构总览

```
.trae/skills/
├── trae-master/              ← 框架本体
│   ├── SKILL.md              ← 入口文件（AI首先读取）
│   ├── FRAMEWORK.md          ← 架构说明 + 启动前校验清单
│   ├── README.md             ← 快速开始
│   ├── DOCUMENTATION.md      ← 完整文档
│   ├── core/                 ← 规则层
│   │   ├── PROTOCOL.md       ← 通信协议
│   │   ├── dispatch-rules.md ← 调度规则
│   │   ├── acceptance-criteria.md ← 验收标准
│   │   ├── meta-skill/       ← 自我认知与技能注册表
│   │   └── context-log/      ← 自动上下文记录
│   ├── skills/               ← 执行层
│   │   ├── workflow-engine/  ← 工作流引擎（Boss+PM+RD+QA+专员）
│   │   ├── karpathy-guidelines/ ← AI编码最佳实践
│   │   ├── project-manager/  ← Placeholder（待开发）
│   │   ├── developer/        ← Placeholder（待开发）
│   │   └── qa-tester/        ← Placeholder（待开发）
│   └── knowledge/            ← 知识层
│       ├── methods/          ← 方法库
│       └── templates/        ← 模板库
├── boss/SKILL.md             ← Boss智能体（由当前AI担任）
├── pm-leader/SKILL.md        ← PM组长智能体
├── rd-leader/SKILL.md        ← RD组长智能体
└── qa-leader/SKILL.md        ← QA组长智能体
```

---

## 三、角色体系

| 角色 | 由谁担任 | 职责 | 通信范围 |
|------|---------|------|---------|
| Boss | 当前使用的AI | 接收需求、审核产出、阶段切换、数据请求中转 | 用户 + 3个组长 |
| PM Leader | AI调度 | 需求分析、动态调度PM专员 | Boss + PM组内专员 |
| RD Leader | AI调度 | 技术设计、代码优先查询、动态调度RD专员 | Boss + RD组内专员 |
| QA Leader | AI调度 | 测试验证、动态调度QA专员 | Boss + QA组内专员 |
| 专员 | AI调度 | 执行专项任务（需求理解/技术设计/功能测试等） | 同组专员 |

**关键规则**：
- Boss 是唯一枢纽，组长之间不能直接通信
- 专员只能与同组专员通信
- 所有跨组通信必须通过 Boss 中转

---

## 四、工作流程

### 完整流程

```
用户提交需求
    ↓
Boss 接收并解读需求
    ↓
Boss 分配给 PM Leader
    ↓
PM Leader 动态调度 PM 专员（需求理解→数据分析→文档编写→需求审查）
    ↓
PM Leader 汇总提交给 Boss
    ↓
Boss 审核 → 向用户汇报 → 用户确认"可以" → 记录日志 → 校验日志
    ↓
Boss 分配给 RD Leader
    ↓
RD Leader Round 1: 代码查询（项目结构/接口/IDL）← 必须先查代码！
RD Leader Round 2: 技术方案设计（基于代码查询结果）
RD Leader Round 3: 编码实现 + 质量保障
    ↓
RD Leader 汇总提交给 Boss
    ↓
Boss 审核 → 向用户汇报 → 用户确认"可以" → 记录日志 → 校验日志
    ↓
Boss 分配给 QA Leader
    ↓
QA Leader 动态调度 QA 专员（测试用例→功能测试→资金安全测试→测试交付）
    ↓
QA Leader 汇总提交给 Boss
    ↓
Boss 审核 → 向用户汇报 → 用户确认"可以" → 记录日志 → 校验日志
    ↓
🎉 流程完成
```

### 阶段切换前置条件

进入下一阶段**必须同时满足**以下4个条件：

1. 上一阶段 Boss 审核通过
2. 上一阶段用户确认"可以"
3. 上一阶段日志已记录
4. 上一阶段日志已校验

**缺一不可，否则禁止进入下一阶段。**

---

## 五、专员体系

### PM 专员（11个）

| # | 专员 | 调度场景 |
|---|------|---------|
| 01 | 需求理解 | 所有需求必调 |
| 02 | 数据分析 | 需要数据支撑时 |
| 03 | 市场调研 | 需要竞品/市场分析时 |
| 04 | 用户调研 | 需要用户画像/场景分析时 |
| 05 | 合规风控 | 涉及合规/风控时 |
| 06 | 国际化 | 涉及多语言/多市场时 |
| 07 | 数据埋点 | 需要埋点方案时 |
| 08 | 外部合作 | 涉及外部依赖时 |
| 09 | 价值评估 | 需要ROI/优先级评估时 |
| 10 | 文档编写 | 所有需求必调（综合产出） |
| 11 | 需求审查 | 所有需求必调（最终审查） |

### RD 专员（13个）

| # | 专员 | 调度场景 |
|---|------|---------|
| 01 | 技术需求 | 所有需求必调 |
| 02 | 业务逻辑 | 涉及业务流程/状态流转时 |
| 03 | 项目查询 | 所有需求必调（Round 1 代码查询） |
| 04 | API文档 | 涉及接口查询时（Round 1） |
| 05 | 技术设计 | 所有需求必调（Round 2） |
| 06 | 数据库设计 | 涉及存储/表结构变更时 |
| 07 | 数据流设计 | 涉及数据流转/一致性时 |
| 08 | 编码规范 | 所有需求必调 |
| 09 | 编码实现 | 需要写代码时 |
| 10 | 接口联调 | 涉及外部系统对接时 |
| 11 | 质量保障 | 代码完成后必调 |
| 12 | 交付运维 | 需要CI/CD/监控时 |
| 13 | 依赖管理 | 涉及新依赖时 |

### QA 专员（13个）

| # | 专员 | 调度场景 |
|---|------|---------|
| 01 | 测试需求 | 所有需求必调 |
| 02 | 测试用例 | 所有需求必调 |
| 03 | 测试准备 | 需要构造测试数据/配置环境时 |
| 04 | 功能测试 | 所有需求必调 |
| 05 | 资金安全 | 涉及金额/支付/信贷时必调 |
| 06 | 验证测试 | 需要回归/数据一致性验证时 |
| 07 | 性能测试 | 涉及高并发/大数据量时 |
| 08 | 安全测试 | 涉及安全敏感操作时 |
| 09 | 兼容性国际化 | 涉及多市场/多语言/多端时 |
| 10 | 灰度测试 | 需要灰度发布验证时 |
| 11 | 自动化测试 | 需要自动化脚本时 |
| 12 | 缺陷记录 | 发现Bug时 |
| 13 | 测试交付 | 所有需求必调（最终交付） |

---

## 六、核心规则

### 1. 数据真实性（最重要）

**严禁推测、判断、推断、预测任何虚假信息，所有数据必须真实可验证。**

| 场景 | 正确行为 | 错误行为 |
|------|---------|---------|
| 数据缺失 | 立即上报，等待真实数据 | 自行推测或编造数据 |
| 信息不确定 | 明确标注 DATA_MISSING | 凭印象或推断给出结论 |
| 需要额外信息 | 通过层级上报请求用户提供 | 假设或猜测缺失信息 |

### 2. 动态调度

组长根据需求内容决定调度哪些专员，不需要的专员不调度。只有存在依赖关系时才需要等待前置专员完成，无依赖关系的专员可并行执行。

### 3. RD 代码优先

RD 阶段 Round 1 **必须先查代码**（项目结构/接口定义/现有实现/IDL），再设计方案。严禁在未查代码的情况下设计架构方案。若违反，Boss 审核必须打回。

### 4. 用户确认触发记录

只有在用户明确确认"可以"后，才会记录日志到 context-log。Boss 审核通过不等于记录，必须等用户确认。

---

## 七、安装与使用

### 前置条件

- 安装了 Trae IDE（或任何支持 Skills 的 AI IDE）
- 将 `skills/` 目录放置在 `.trae/skills/` 下

### 目录结构要求

```
你的项目/.trae/skills/
├── trae-master/     ← 框架本体
├── boss/            ← Boss智能体
├── pm-leader/       ← PM组长
├── rd-leader/       ← RD组长
└── qa-leader/       ← QA组长
```

### 使用步骤

1. **启动框架**：在对话中提及需要执行研发工作流，AI 会读取框架文件
2. **完成启动前校验**：AI 会按照 FRAMEWORK.md 中的 Pre-flight Check 读取所有框架文件
3. **提交需求**：向 AI 描述你的需求，AI 以 Boss 身份接收
4. **跟进流程**：AI 会按 PM → RD → QA 顺序执行，每个阶段结束会向你汇报
5. **确认或打回**：每个阶段汇报后，你确认"可以"才会进入下一阶段
6. **流程完成**：三个阶段全部通过后，AI 输出完成报告

### 自定义知识库

框架默认不包含业务域知识库。你可以创建自己的知识库：

1. 在 `trae-master/knowledge/` 下创建新目录，如 `my-domain/`
2. 创建 `SKILL.md` 文件，添加 frontmatter：
   ```yaml
   ---
   name: "my-domain"
   description: "Your domain knowledge description"
   ---
   ```
3. 在 `core/meta-skill/REGISTRY.md` 中注册，状态设为 Active
4. 在 `FRAMEWORK.md` 的 Pre-flight Check 中添加读取项

---

## 八、审核与打回机制

### Boss 审核流程

每个阶段完成后：

1. 组长汇总所有专员产出，提交给 Boss
2. Boss 执行数据真实性校验：
   - 所有专员产出的数据是否有明确来源？
   - 是否存在未标注的推断/推测/预测内容？
   - 缺失数据是否已标注 DATA_MISSING？
   - 关键数据是否与原文一致？
3. Boss 通过 → 向用户汇报 → 用户确认 → 记录日志 → 下一阶段
4. Boss 不通过 → 附带错误包打回组长 → 组长重新调度相关专员

### 3次拒绝机制

同一阶段 Boss 最多拒绝3次。第3次拒绝后，Boss 向用户提供选项：

| 选项 | 说明 |
|------|------|
| 重试(Retry) | 从头重启本阶段 |
| 跳过(Skip) | 跳过本需求，继续下一个 |
| 简化(Simplify) | 缩减范围以适应限制 |
| 终止(Abort) | 停止工作流，记录失败 |

---

## 九、数据缺失处理

当专员报告数据缺失时，按以下链路处理：

```
专员报告 DATA_MISSING
    ↓
组长汇总所有数据缺失请求
    ↓
组长在汇总输出中列出所有缺失数据
    ↓
Boss 汇总后向用户请求数据
    ↓
用户提供数据
    ↓
Boss 转发给组长
    ↓
组长继续执行
```

**严禁任何角色自行编造或推测缺失数据。**

---

## 十、需求记录

每个需求会在 `skills/workflow-engine/requirements/active/REQ-YYYY-NNN/` 下创建目录，包含：

| 文件 | 说明 |
|------|------|
| metadata.md | 需求基本信息（ID、标题、优先级等） |
| prd.md | PRD文档 |
| pm-record.md | PM阶段记录 |
| rd-record.md | RD阶段记录 |
| qa-record.md | QA阶段记录 |
| timeline.md | 完整时间线 |

需求完成后会移动到 `requirements/completed/` 目录。

---

## 十一、常见问题

**Q: 可以跳过某个阶段吗？**
A: 不可以。PM → RD → QA 是严格顺序，每个阶段都是下一阶段的输入。

**Q: 可以只调用某个组长吗？**
A: 可以。pm-leader、rd-leader、qa-leader 可以独立调用，但此时没有 Boss 审核，产出质量由你自行把控。

**Q: 专员是固定的还是动态的？**
A: 动态的。组长根据需求内容决定调度哪些专员，不需要的专员不调度。

**Q: RD 阶段为什么必须先查代码？**
A: 避免脱离项目实际状态凭空设计方案。先查代码了解现有实现，再基于真实代码设计方案，确保方案可落地。

**Q: 数据缺失时怎么办？**
A: 立即标注 DATA_MISSING 并上报，等待用户提供真实数据。绝不允许自行推测或编造。

**Q: 如何添加自己的业务知识库？**
A: 参见"自定义知识库"章节，在 knowledge/ 下创建目录并注册到 REGISTRY.md。
